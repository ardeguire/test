{% extends 'base.html.twig' %}

{%  block body %}
    <h1>Hello, {{ name }}!</h1>
    <div id="nav">
        <h3>Navigation</h3>
        <ul>
            <li><a href="{{ logout_path(key= null) }}">Logout</a></li>
            <li><a href="{{ url("admin") }}">Dashboard</a></li>
        </ul>
    </div>
    <p>Your lucky number is {{ number }}.</p>
    <p>Your role array is: {{ roles }}</p>
{% endblock %}